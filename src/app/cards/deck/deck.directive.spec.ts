import { DeckDirective } from './deck.directive';

describe('DeckDirective', () => {
  it('should create an instance', () => {
    const directive = new DeckDirective();
    expect(directive).toBeTruthy();
  });
});
